version: '3.4'

services:
  springboot-service:
    image: docker.nexus.archi-lab.io/archilab/coalbase-comment-dev
    environment:
      - TZ=Europe/Berlin
      - SPRING_PROFILES_ACTIVE=dev
    ports:
      - "8083:8080"
